import React from 'react';

const Navigation = () => {
  const navigationStyles = {
    // fixed
    width: '100%',
    height: '8vh',
    backgroundColor: '#85cad4',
    // opacity: .5,
  };

  return <div className="navigation" style={navigationStyles}></div>;
};

export default Navigation;
